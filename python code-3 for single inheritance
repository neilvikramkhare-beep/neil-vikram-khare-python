class Employee:
    def __intit__(self,role,dept,salary):
        self.role=role
        self.dept=dept
        self.salary=salary
        def showDetails(self):
            print("Role=",self.role)
            print("Department=",self.dept)
            print("Salary",self.salary)
              class Engineer(Employee):
                def __intit__(self,name,age):
                        self.name=name
                        self.age=age
                        super().__init__("Chemical engineer","Forensics",60000)
                        engg1 = Engineeer("Rahul Malhotra",87)
                        engg1.showDetails()
