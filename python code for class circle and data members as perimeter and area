class Circle:
    def __init__(self,radius):
        self.radius=radius
        def area(self):
            return self.radius ** 2 * 3.14 
            def perimter(self):
                return 2 * 3.14 * self.radius
                c1=Circle(87)
                print(c1.area)
                print(c1.perimeter)
